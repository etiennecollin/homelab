---
services:
  ddclient:
    image: "lscr.io/linuxserver/ddclient:4.0.0"
    container_name: "ddclient"
    environment:
      PUID: "${PUID}"
      PGID: "${PGID}"
      TZ: "${TIMEZONE}"
    volumes:
      - "./config:/config"
    restart: "unless-stopped"
